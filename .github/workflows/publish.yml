name: Publish to npm

on:
  push:
    branches: [main]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Integration Tests
        run: npm run test:integration
        env:
          FIREBASE_API_KEY: ${{ secrets.E1_FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.E1_FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ secrets.E1_FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.E1_FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.E1_FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.E1_FIREBASE_APP_ID }}
          FIREBASE_ADMIN_SERVICE_ACCOUNT_KEY: ${{ secrets.E1_FIREBASE_ADMIN_SERVICE_ACCOUNT_KEY }}
          OWNER_EMAILS: ${{ secrets.E1_OWNER_EMAILS }}

      - name: Check if version changed
        id: version
        run: |
          CURRENT=$(node -p "require('./package.json').version")
          PREVIOUS=$(git show HEAD~1:package.json 2>/dev/null | node -p "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')).version" 2>/dev/null || echo "")
          if [ "$CURRENT" != "$PREVIOUS" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "version=$CURRENT" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish
        if: steps.version.outputs.changed == 'true'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: |
          if [ "${{ steps.version.outputs.changed }}" == "true" ]; then
            echo "### 📦 Published to npm" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "**Package**: \`@prmichaelsen/agentbase-core@${{ steps.version.outputs.version }}\`" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "[View on npm](https://www.npmjs.com/package/@prmichaelsen/agentbase-core/v/${{ steps.version.outputs.version }})" >> "$GITHUB_STEP_SUMMARY"
          else
            CURRENT=$(node -p "require('./package.json').version")
            echo "### ⏭️ Publish skipped" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Version \`$CURRENT\` unchanged — no publish needed." >> "$GITHUB_STEP_SUMMARY"
          fi
